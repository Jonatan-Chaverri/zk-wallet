{
  "name": "zk-wallet-test",
  "version": "1.0.0",
  "type": "module",
  "description": "Testing scripts for zkWallet",
  "scripts": {
    "test": "node test-proof-only.mjs",
    "test:full": "node test-complete-flow.mjs",
    "test:circuit": "cd wallet_proof && nargo test",
    "compile:circuit": "cd wallet_proof && nargo compile",
    "compile:and:test": "cd wallet_proof && nargo compile && cd .. && node test-proof-only.mjs"
  },
  "dependencies": {
    "@aztec/bb.js": "^2.0.4",
    "@noir-lang/noir_js": "1.0.0-beta.14",
    "circomlibjs": "^0.1.7"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
