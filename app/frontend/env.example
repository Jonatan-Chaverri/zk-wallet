# Backend API URL
# All other configuration (RPC URL, contract addresses, etc.) is now fetched from the backend API
NEXT_PUBLIC_API_URL=http://localhost:3001

# Note: CHAIN_ID is fetched from backend but kept in frontend state for Noir proof generation
# This is the only exception - all other config comes from the backend API